import React from 'react';

export const DocCol = (props) => <div
    className="doc doc-col">{props.children}</div>;

export const DocRow = (props) => <div
    className="doc doc-row">{props.children}</div>;
